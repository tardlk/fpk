[
    {
        "stepTitle": "FNet 网络工具",
        "items": [
            {
                "type": "tips",
                "helpText": "FNet 是飞牛 NAS 网络优化工具。<br><br><b>功能：</b><br>1. TCP BBR 拥塞控制 — 一键开关，持久化到 sysctl.conf<br>2. Hosts 编辑 — 直接编辑 /etc/hosts 文件<br><br><b>注意：</b>BBR 启用需要加载内核模块 tcp_bbr，如内核不支持则无法开启。写入 sysctl.conf 和 /etc/hosts 需要 root 权限。"
            }
        ]
    }
]
